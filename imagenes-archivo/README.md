# Imágenes archivadas (no publicadas)

Originales que **ninguna página del sitio referencia** (verificado contra el
HTML generado y todo `src/`). Se sacaron de `public/` el 2026-06-11 para
reducir el deploy de 132 MB a ~51 MB.

Para publicar una imagen de aquí: muévela de vuelta a `public/imagenes/<ruta>`
y referénciala desde el código o contenido.
